using System;
using System.Collections.Generic;
using System.Text;

// this is just a one stop location for placing enums 


namespace Data
{
    public enum AddressTypeUser { Home,Work,Contact,Other  }
    public enum AppAdminRole { AppAdministrator, Administrator, Manager, User }
    public enum AppRole { ApplicationAdministrator, Administrator, Manager, User }
}
